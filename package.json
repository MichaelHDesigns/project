{
  "name": "my-project",
  "version": "1.0.0",
  "description": "My Hardhat project",
  "scripts": {
    "clean": "rm -rf build",
    "compile": "npx hardhat compile",
    "test": "npx hardhat test",
    "deploy:localhost": "npx hardhat run --network localhost scripts/deploy.js"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.4.0",
    "ethers": "^5.4.4",
    "hardhat": "^2.9.0",
    "hardhat-deploy": "^0.10.0",
    "hardhat-deploy-ethers": "^2.0.1",
    "hardhat-deploy-solc": "^2.0.0",
    "hardhat-ethers": "^2.0.1",
    "hardhat-waffle": "^2.0.1"
  }
}
